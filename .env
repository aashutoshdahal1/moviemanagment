# Disable minification during development to avoid evaluation issues
GENERATE_SOURCEMAP=false
INLINE_RUNTIME_CHUNK=false

# Fix for Node.js v22+ localStorage security error
NODE_OPTIONS=--no-experimental-global-customevent --no-experimental-global-navigator
